import { useMemo, useState, type FormEvent } from 'react'
import { Link, useParams } from 'react-router-dom'
import { useAuth } from '../auth/useAuth'
import { AdminCreateDeviceForm, AdminDeviceForms } from './AdminDeviceForms'
import { createComment } from './commentRepository'
import { archiveDevice } from './deviceRepository'
import { buildActivityTimeline, filterDevices } from './deviceUtils'
import type { DeviceFilters } from './models'
import { useDeviceDetails, useDevices } from './useDeviceData'
import { validateCommentInput } from './validation'

const typeLabels={ambona:'Ambona',zwyzka:'Zwyżka',pasnik:'Paśnik',lizawka:'Lizawka',inne:'Inne'}
const statusLabels={sprawne:'Sprawne',wymaga_naprawy:'Wymaga naprawy',wylaczone:'Wyłączone z użytkowania',archiwalne:'Archiwalne'}
const formatDate=(value:{toDate:()=>Date}|null)=>value?.toDate().toLocaleDateString('pl-PL')??'Brak'

export function DevicesRegistryPage(){
 const {devices,loading,error}=useDevices();const {role,user}=useAuth();const isAdmin=role==='admin';const [showCreate,setShowCreate]=useState(false);const [filters,setFilters]=useState<DeviceFilters>({district:'',type:'',status:'',guardian:'',needsRepair:false});const filtered=useMemo(()=>filterDevices(devices,filters),[devices,filters]);const guardians=[...new Set(devices.map(x=>x.guardianName).filter(Boolean))] as string[]
 return <section className="member-page"><div className="container member-shell registry-shell"><div className="registry-heading"><div><span className="eyebrow">Strefa członkowska</span><h1>Przegląd urządzeń łowieckich</h1></div>{isAdmin&&<button className="button" type="button" onClick={()=>setShowCreate(x=>!x)}>Dodaj urządzenie</button>}</div>{isAdmin&&showCreate&&<AdminCreateDeviceForm uid={user!.uid}/>}<Link to="/panel">← Wróć do panelu</Link><div className="device-filters" aria-label="Filtry urządzeń"><label>Obwód<select value={filters.district} onChange={e=>setFilters({...filters,district:e.target.value})}><option value="">Wszystkie</option>{[...new Set(devices.map(x=>x.districtNumber).filter(x=>x!==null))].map(x=><option key={x} value={String(x)}>{x}</option>)}</select></label><label>Typ<select value={filters.type} onChange={e=>setFilters({...filters,type:e.target.value})}><option value="">Wszystkie</option>{Object.entries(typeLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Status<select value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">Wszystkie</option>{Object.entries(statusLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Opiekun urządzenia<select value={filters.guardian} onChange={e=>setFilters({...filters,guardian:e.target.value})}><option value="">Wszyscy</option>{guardians.map(x=><option key={x}>{x}</option>)}</select></label><label className="check-filter"><input type="checkbox" checked={filters.needsRepair} onChange={e=>setFilters({...filters,needsRepair:e.target.checked})}/> Tylko wymagające naprawy</label></div>{loading&&<div className="auth-loading" role="status">Ładowanie rejestru…</div>}{error&&<div className="form-message error" role="alert">{error}</div>}{!loading&&!error&&filtered.length===0&&<div className="empty-state"><h2>Brak urządzeń spełniających kryteria</h2><p>Rejestr nie zawiera jeszcze danych lub filtry nie zwróciły wyników.</p></div>}<div className="device-list">{filtered.map(d=><article className="device-row" key={d.id}><div className="device-photo-placeholder" aria-hidden="true">Zdjęcie</div><div><span className={`status-badge ${d.status}`}>{statusLabels[d.status]}</span><h2>Nr {d.number} — {d.name}</h2><dl><Metric label="Typ" value={typeLabels[d.type]}/><Metric label="Obwód" value={d.districtNumber??'—'}/><Metric label="Opiekun urządzenia" value={d.guardianName??'Nieprzypisany'}/><Metric label="Ocena" value={`${d.conditionScore}/5`}/><Metric label="Ostatni przegląd" value={formatDate(d.latestInspectionAt)}/><Metric label="Otwarte usterki" value={d.openIssuesCount}/></dl>{d.status==='wylaczone'&&<strong className="safety-warning">Urządzenie wyłączone z użytkowania</strong>}<div className="row-actions"><Link className="button outline" to={`/panel/urzadzenia/${d.id}`}>Otwórz szczegóły</Link>{isAdmin&&<button type="button" onClick={()=>{if(confirm('Czy zarchiwizować urządzenie?'))void archiveDevice(d.id,user!.uid)}}>Archiwizuj</button>}</div></div></article>)}</div></div></section>
}

export function DeviceDetailsPage(){
 const {id=''}=useParams();const {device,inspections,issues,comments,repairs,media,loading,error}=useDeviceDetails(id);const {user,profile,role}=useAuth();const [content,setContent]=useState('');const [type,setType]=useState<'uwaga'|'problem'|'propozycja'|'informacja'|'zgloszenie_naprawy'>('uwaga');const [message,setMessage]=useState('');const timeline=useMemo(()=>buildActivityTimeline({inspections,issues,repairs,comments,media}),[inspections,issues,repairs,comments,media]);async function submit(e:FormEvent){e.preventDefault();if(Object.keys(validateCommentInput({content})).length){setMessage('Treść zgłoszenia jest wymagana.');return}try{await createComment(id,{type,content,relatedType:'device',relatedId:null},{uid:user!.uid,name:profile?.displayName||user!.displayName||'Członek'});setContent('');setMessage('Zgłoszenie zostało zapisane.')}catch{setMessage('Nie udało się zapisać zgłoszenia.')}}
 if(loading)return <div className="auth-loading" role="status">Ładowanie urządzenia…</div>;if(!device)return <section className="member-page"><div className="container member-shell"><div className="form-message error">{error||'Nie znaleziono urządzenia.'}</div><Link to="/panel/urzadzenia">← Wróć do rejestru</Link></div></section>;const current=media.find(x=>x.id===device.currentPhotoId&&!x.hidden);const open=issues.filter(x=>!['usunieta','odrzucona'].includes(x.status));const memberName=profile?.displayName||user!.displayName||'Administrator'
 return <section className="member-page"><div className="container member-shell registry-shell"><Link to="/panel/urzadzenia">← Wróć do rejestru</Link>{error&&<div className="form-message error">{error}</div>}<header className="device-detail-hero">{current?<img src={current.path} alt={current.caption||`Urządzenie ${device.number}`}/>:<div className="device-photo-placeholder">Brak aktualnego zdjęcia</div>}<div><span className={`status-badge ${device.status}`}>{statusLabels[device.status]}</span><h1>Nr {device.number} — {device.name}</h1><strong>Ocena: {device.conditionScore}/5</strong></div></header><section className="registry-section"><h2>Metryczka urządzenia</h2><dl className="device-metrics"><Metric label="Typ" value={typeLabels[device.type]}/><Metric label="Obwód" value={device.districtNumber??'—'}/><Metric label="Status" value={statusLabels[device.status]}/><Metric label="Wersja" value={device.version}/></dl></section><section className="registry-section"><h2>Opiekun urządzenia</h2><p>{device.guardianName??'Nie przypisano'}</p></section><a className="button outline" href={`https://www.google.com/maps?q=${device.location.latitude},${device.location.longitude}`} target="_blank" rel="noreferrer">Otwórz w Google Maps</a><section className="registry-section"><h2>Otwarte usterki i prace</h2>{open.length?open.map(x=><article className="history-card" key={x.id}><strong>{x.title}</strong><p>{x.description}</p><span>{x.priority}</span></article>):<p>Brak otwartych usterek.</p>}</section><section className="registry-section"><h2>Ostatni przegląd</h2>{inspections[0]?<article className="history-card"><strong>{formatDate(inspections[0].inspectionDate)} · {inspections[0].conditionScore}/5</strong><p>{inspections[0].description}</p></article>:<p>Brak przeglądów.</p>}</section><section className="registry-section"><h2>Zgłoś uwagę</h2><form className="registry-form" onSubmit={submit}><label>Rodzaj<select value={type} onChange={e=>setType(e.target.value as typeof type)}><option value="uwaga">Uwaga</option><option value="problem">Problem</option><option value="propozycja">Propozycja</option><option value="informacja">Informacja</option><option value="zgloszenie_naprawy">Zgłoszenie naprawy</option></select></label><label>Treść<textarea value={content} onChange={e=>setContent(e.target.value)} required/></label><button className="button" type="submit">Wyślij zgłoszenie</button>{message&&<p role="status">{message}</p>}</form></section><History title="Komentarze członków" items={comments.filter(x=>x.status!=='ukryty').map(x=>({id:x.id,date:x.createdAt,text:x.content}))}/><History title="Historia przeglądów" items={inspections.map(x=>({id:x.id,date:x.inspectionDate,text:x.description}))}/><History title="Historia napraw" items={repairs.map(x=>({id:x.id,date:x.completedAt,text:x.description}))}/><section className="registry-section"><h2>Zdjęcia aktualne i historyczne</h2><div className="media-grid">{media.filter(x=>!x.hidden).map(x=><figure key={x.id}><img src={x.path} alt={x.caption||'Zdjęcie urządzenia'}/><figcaption>{x.caption||x.category}{x.isCurrent?' · aktualne':''}</figcaption></figure>)}</div></section><section className="registry-section"><h2>Oś aktywności</h2><ol className="timeline">{timeline.map(x=><li key={`${x.kind}-${x.id}`}><time>{formatDate(x.date)}</time><strong>{x.title}</strong></li>)}</ol></section>{role==='admin'&&<section className="registry-section"><AdminDeviceForms deviceId={id} uid={user!.uid} name={memberName} issues={issues} comments={comments} currentPhotoId={device.currentPhotoId}/></section>}</div></section>
}
function Metric({label,value}:{label:string;value:string|number}){return <div><dt>{label}</dt><dd>{value}</dd></div>}
function History({title,items}:{title:string;items:{id:string;date:{toDate:()=>Date};text:string}[]}){return <section className="registry-section"><h2>{title}</h2>{items.length?items.map(x=><article className="history-card" key={x.id}><time>{formatDate(x.date)}</time><p>{x.text}</p></article>):<p>Brak wpisów.</p>}</section>}
